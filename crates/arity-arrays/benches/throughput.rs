//! Throughput benchmarks for `PackedArray` against the alternatives a consumer
//! would otherwise reach for. The bench bodies (added in later tasks) build on
//! the shared support module, which the `bench_internals` integration test
//! also includes and unit-tests.

fn main() {
    divan::main();
}
